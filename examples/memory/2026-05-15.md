# 2026-05-15 (UTC)

## Context

Collected a completed task-only docs folder into memory so `docs/` stays focused on durable task-scoped documentation.

## Observations

- Refactored `design-api` into a router skill.
- Added OpenAPI, AsyncAPI, and GraphQL references.
- Expanded eval coverage to at least 10 cases.
- Ran skill validation and reviewed the final diff.
- Created a durable Agent PDLC diagram in `pages/AGENT_PDLC.md`.
- Tightened `operator-git` / `git-message` instructions to infer commit-message style from the target repository instead of using repo-specific examples.
- Added `examples/skills/codegen-backend` as a router skill for backend implementation across 20 TIOBE-informed backend routes, with 8 evals per route.
- Tightened every `codegen-backend` language reference with route-specific backend best practices and verification guidance.

## Next Steps

- [x] Remove the task-only docs folder once its notes are preserved in memory.
- [x] Add skill length budgets to the agent playbook docs: metadata <= 100 tokens, instructions <= 5000 tokens.
- [x] Refactor example skills toward the length budgets; keep router skills but shorten metadata and split `creator-skill` guidance into references.
- [x] Retest skill definitions with `creator-skill` validation; old and new eval files live under ignored `examples/skills/*/evals/` directories.
- [x] Create the P1 `codegen-backend` skill from `docs/2026-05-02-team-roles-as-skills`.
