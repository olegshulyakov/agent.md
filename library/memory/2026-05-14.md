# 2026-05-14 (UTC)

## Context

Collected completed task-only docs folders into memory so `docs/` can stay focused on project docs and task folders with durable PRD, SPEC, architecture, or design artifacts.

## Observations

- Created `library/skills/creator-rule/SKILL.md` after reading the playbook rule template and local skill conventions.
- Added creator-rule eval prompts, local skill validation, trigger evals, validation workflow documentation, output grading scripts, and local eval validation.
- Updated `library/skills/creator-skill` to support multiple agent runtimes, configurable runner options where practical, and eval/iteration paths inside the target skill folder.
- Adjusted creator-skill layout guidance to avoid unnecessary empty resource folders and checked reference path consistency.
- Added a pull request workflow for changed Markdown files, limited lint reporting to changed lines, moved workflow Bash and JavaScript into `.devops/markdown-format`, added npm cache configuration, and validated workflow/script syntax locally.
- Updated `operator-git` behavior by preserving branch naming and commit message convention references, defining output and action workflows, removing prompt examples from router instructions, adding eval prompts, deterministic contract checks, and stale documentation fixes.
- Moved GitHub Pages files into `pages/`, updated repository links, added a path-filtered Pages workflow, and verified repository status.
- Wrote `library/skills/README.md` for setup, authoring, evals, packaging, and troubleshooting; also fixed validation drift in the documented packaging path and verified links, validation, and helper command behavior.
- Added a concise token-efficiency rule that preserves logic and useful detail.

## Next Steps

- [x] Remove task-only docs folders once their notes are preserved in memory.
